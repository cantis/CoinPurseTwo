services:
  web:
    build: ./web
    container_name: web
    restart: always
    environment:
      - APP_NAME=CoinPurse
    expose:
      - 8080

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - 80:80
    depends_on:
      - web
